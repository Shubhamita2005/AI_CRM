# AI_CRM

AI_CRM is an AI-powered Sales CRM designed to help PLG SaaS startups manage leads, customers, and sales efficiently.

---

## Clone the Repository

Clone the project from GitHub:

```bash
git clone https://github.com/Shubhamita2005/AI_CRM.git
```

Go to the project folder:

```bash
cd AI_CRM
```

---

## Project Structure

```
AI_CRM/
│
├── frontend/      # React Frontend (Vite)
├── backend/       # Backend
└── README.md
```

---

## Frontend Setup (React + Vite)

Move to the frontend folder:

```bash
cd frontend
```

Install all dependencies:

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

Open the URL displayed in the terminal (usually http://localhost:5173).

---

## Creating the React Frontend (For New Developers)

The frontend was created using Vite.

Navigate to the frontend folder:

```bash
cd frontend
```

Create the React project:

```bash
npm create vite@latest .
```

Select:

- Framework: **React**
- Variant: **JavaScript**
- Linter: **ESLint**

Install dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

---

## Push Changes

After making changes:

```bash
git add .
git commit -m "Your commit message"
git push
```

---

## Technologies Used

- React
- Vite
- JavaScript
- ESLint
- Git
- GitHub
